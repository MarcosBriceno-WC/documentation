

# Connect MCP

briefly introduce the page and explain the three types of MCP connections available: MCP Gateway, Dynamic MCP, and MCP CLI.

## Prerequisites 

list what the user needs before getting started.

## MCP Gateway Connection 

sStart with a short introduction explaining what the MCP Gateway is, why it works the way it does, and why it is the simplest connection method. Follow with the step-by-step instructions for this connection type.

When explaining the configure yor client list, do noot use the steps components inside the client tab instead use numebered list.

## Dynamic MCP Connection

short intro explaining what Dynamic MCP is, followed by its steps.

## MCP CLI Connection

short intro explaining what the MCP CLI connection is, followed by its steps.
Verify It Is Working — a section explaining how the user can confirm their connection is working correctly.
## Troubleshooting 
use Mintlify's <Accordion> component to present each issue as an expandable FAQ item.

## Next Steps 

a brief closing section pointing the user to what they should do after completing the setup. using cards component